---
env:
  MIX_ENV: prod
applications:
- name: cf_phoenix
  memory: 1024M
  instances: 1
  host: cf-phoenix
  domain: cfapps.io
  buildpack: https://github.com/HashNuke/heroku-buildpack-elixir.git
  command: ls priv/static && ls priv/static/js && mix phoenix.server
